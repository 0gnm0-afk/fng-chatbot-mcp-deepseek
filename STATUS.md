# STATUS — F&G 시장심리 MCP 챗봇

> 마지막 갱신: 2026-07-21 (도구: Claude Code)

## 현재 상태

- CNN Fear & Greed 종합점수와 7개 구성 지표, 주요 시장시세를 Python으로 수집·정규화한다.
- Python 규칙이 공포 요인·완충 요인·한 줄 해석을 만들고, Telegram 미리보기에서만 DeepSeek가 선택 요인의 의미를 보조 설명한다. DeepSeek 실패 시 규칙 전용 미리보기로 복귀한다.
- 로컬 FastMCP stdio 서버가 보고서 조회, Telegram 미리보기, 승인 전송 도구 3개를 제공한다.
- Codex는 `.codex/config.toml`과 `.agents/skills/fear-greed-report/`, Claude Code는 `.mcp.json`과 `.claude/skills/fear-greed-report/`로 같은 MCP와 Skill을 사용한다.
- Google Cloud, HTTP 앱, Docker, Gemini 실행 경로는 제거 상태를 유지하고, DeepSeek V4 Flash는 별도 SDK 없이 기존 `httpx`로 NVIDIA hosted NIM을 호출한다.
- Telegram 전송은 미리보기 원문·SHA-256·명시적 승인·서버 활성화가 모두 맞아야 실행된다.

## 작업 큐


- [완료] 사용자가 Codex가 표시한 Telegram 미리보기(해시 `8406f767c572c19c6baa9b8b766d5cfa15d4542083fb7b054924880bd0bca3c0`)를 수정 없이 전송 승인해, 동일 원문·해시로 `send_telegram_report`를 호출했다. 전송 전 재구성 문자열과 새 미리보기는 해시 불일치로 중단했고, 최종 전송은 UTF-8 파일에서 복원한 원문이 승인 해시와 일치할 때만 수행했다. 전송 결과는 `message_id=320`, `idempotency_key=daily-report:2026-07-21`, `sent=true`다. (2026-07-21, Codex)
- [완료] 이전 세션이 남긴 승인 대기 항목(해시 `8406f767c572c19c6baa9b8b766d5cfa15d4542083fb7b054924880bd0bca3c0`)은 세션 사이 시장 데이터가 갱신되며 유효하지 않게 되어 그대로 이어받지 않았다. 이번 세션에서 `fear-greed-report` Skill로 보고서를 새로 조회·검증하고 DeepSeek 설명이 적용된(`status=applied`, `source=deepseek`) 새 Telegram 미리보기를 생성해 사용자에게 제시했다. 사용자가 수정 없이 전송을 명시 승인해 동일 원문·해시로 `send_telegram_report`를 호출했다. 미리보기 SHA-256은 `7609d4f2bc61252694bc658844c8b2a6337acfc565f4aedb9be73ee5a66af0f3`이고, 전송 결과는 `message_id=319`, `idempotency_key=daily-report:2026-07-21`, `sent=true`다. (2026-07-21, Claude)
- [완료] `fear-greed-report` Skill로 현재 보고서를 조회하고 7개 지표·Python 선정 요인·DeepSeek 적용 여부를 검증한 뒤 Telegram 미리보기를 전송 없이 제시했다. CNN 조회와 NVIDIA DeepSeek 호출은 HTTP 200이었고, `interpretation.status=applied`, `source=deepseek`, 참조 ID 순서 `stock_price_breadth`, `put_call_options`, `market_volatility`가 Python 선정 요인과 일치했다. Telegram 미리보기 해시는 `8406f767c572c19c6baa9b8b766d5cfa15d4542083fb7b054924880bd0bca3c0`, `sent=false`이며 실제 Telegram 전송은 수행하지 않았다. (2026-07-21, Codex)
- [완료] 사용자 결정에 따라 `_FORBIDDEN_PHRASES` 금지 표현 검증을 전부 제거했다. 실측 진단 결과 5회 중 2회를 막은 것은 `매도` 하나였고, 걸린 문장은 "하락 종목 거래량이 상승 종목을 크게 압도해 매도 심리가 극도로 확산되었습니다"처럼 매도를 권유하는 문장이 아니라 시장 상황을 서술하는 정상 출력이었다. 서술형 허용·권유형 차단으로 좁히는 대안을 제시했으나 사용자는 전체 삭제를 선택했다. 상수·검증 블록·`validation_forbidden_language` 사유 코드·해당 테스트와 현재 동작을 기술한 문서를 제거하고, 시스템 프롬프트의 금지 지시는 남긴다. 날짜가 붙은 DECISIONS 기록은 위조하지 않고 새 결정으로 대체 기록한다. (2026-07-21, Claude)
- [완료] `http_503` 실원인 확정에 따라 DeepSeek 재시도 정책을 수정했다. 실측 진단으로 키·모델·payload는 모두 정상(앱과 동일한 요청이 HTTP 200)이고, 503 본문이 `ResourceExhausted: Worker local total request limit reached (48/48)`인 공용 추론 풀 포화임을 확인했다. 현재 실패의 직접 원인은 `DEEPSEEK_MAX_ATTEMPTS=2`와 대기 없는 즉시 재시도라 포화된 워커를 그대로 재타격하는 구조다(실측: 무대기 2회 5/6, 백오프 4회 6/6). 1) 재시도 사이 지수 backoff+jitter 추가, 2) 최대 시도 2→4회 상향과 MCP 90초 예산을 지키는 전체 시간 budget 추가, 3) 공급자 오류 본문을 고정 어휘 사유 코드로 분류해 용량 문제와 설정 문제를 구분 가능하게 한다. 오류 원문과 비밀값은 그대로 보존하지 않고 Telegram 본문에도 넣지 않는다. (2026-07-21, Claude)
- [완료] NVIDIA hosted DeepSeek `http_503`을 코드 변경 없이 진단했다. 직전 HTTP 200 성공과 현재 반복 503을 함께 보면 키·endpoint·요청 형식보다 hosted 모델의 일시적 비가용·용량 포화 가능성이 높다. 현재 클라이언트는 5xx를 총 2회 시도하지만 대기와 `Retry-After` 처리가 없어 즉시 연속 실패할 수 있다. 운영 복구는 잠시 후 새 미리보기, NVIDIA 모델 playground 교차 확인, 장기 지속 시 요청 식별정보와 UTC 시각을 포함한 지원 문의 순서이며, 코드 완화는 `Retry-After` 우선·지수 backoff와 jitter·제한된 3회 시도·규칙 fallback 유지가 적절하다. 실제 Telegram 전송과 코드 변경은 수행하지 않았다. (2026-07-21, Codex)
- [중단] `fear-greed-report` Skill로 현재 보고서를 조회해 7개 고유 지표·완전성·최신성·규칙 요약·Python 선정 요인 일치를 확인하고 Telegram 미리보기를 생성했다. DeepSeek는 세 번의 미리보기에서 각 제한 재시도까지 모두 NVIDIA `http_503`으로 규칙 fallback되어 설명 포함 요구를 달성하지 못했다. 최종 미리보기 해시는 `ec95cc7f1e15c356f011ab74a344cc26a79731c8219c213f94351b8c87d7ad50`, `sent=false`이며 실제 Telegram 전송은 수행하지 않았다. (2026-07-21, Codex)
- [완료] DeepSeek 미리보기 실패 진단을 우선순위대로 개선했다. 1) 해석 검증 오류를 JSON·키·타입·줄·길이·ID·금지 표현별 안전 코드로 분류, 2) `httpx` 전송 오류를 timeout·connect/read/write/close/proxy/network별 안전 코드로 분류, 3) MCP fallback 메타데이터에 연결, 4) 실진단에서 확인한 read timeout과 HTTP 503에 따라 요청별 20초 timeout 및 read timeout·429·5xx 총 2회 제한 재시도를 적용했다. 최종 실미리보기는 HTTP 200, DeepSeek 설명 2줄·참조 ID 순서 일치, SHA-256 `76b3825f2e23ec8beb797c08a4eb293404f51a43f74266273cdc5363401636f1`, `sent=false`였다. 전체 150개 테스트, Ruff lint·format, 두 Skill·두 MCP 설정 계약, 비밀값·개인 절대경로 검색, scoped diff 검증을 통과했고 실제 Telegram 전송은 수행하지 않았다. (2026-07-21, Codex)
- [완료] 직전 DeepSeek 미리보기 실패가 사용자 환경설정 문제인지 코드 문제인지 판별했다. 현재 프로세스의 `NVIDIA_API_KEY`, NVIDIA endpoint와 모델·인증은 첫 실호출 HTTP 200으로 정상임이 확인됐고 `.codex/config.toml`도 해당 변수명을 전달하므로 환경설정 재작성은 우선 조치가 아니다. 먼저 `InterpretationValidationError`와 `httpx.RequestError`를 비밀값 없는 세부 사유 코드로 분류해 관측성을 높인 뒤, 확인된 하위 원인에 따라 프롬프트·파서·10초 timeout·미리보기 전용 제한 재시도를 조정하는 코드 수정이 필요하다. 진단만 수행했으며 코드와 환경변수는 변경하지 않았다. (2026-07-21, Codex)
- [완료] 직전 `fear-greed-report` 미리보기의 DeepSeek 실행 경계를 확인했다. 첫 호출은 NVIDIA endpoint가 HTTP 200을 반환해 호출·인증·HTTP 응답까지 성공했지만 completion 내용이 `interpretation.py`의 JSON·필드·줄 수·길이·참조 ID 순서·금지 표현 계약 중 하나를 위반해 `validation_error`로 폐기됐다. 후속 두 호출은 `httpx.RequestError` 계열의 `transport_error`로 usable HTTP 응답을 얻지 못했다. 비밀값·원시 공급자 오류·원시 모델 출력은 의도적으로 보존하지 않아 세부 위반 항목이나 전송 오류 종류는 현재 기록만으로 특정할 수 없다. 코드 변경이나 Telegram 전송은 수행하지 않았다. (2026-07-21, Codex)
- [중단] `fear-greed-report` Skill로 현재 보고서를 조회해 7개 고유 지표·완전성·최신성·규칙 요약·Python 선정 요인 일치를 확인하고 Telegram 미리보기를 생성했다. DeepSeek 호출은 첫 미리보기에서 HTTP 200 뒤 `validation_error`, 후속 두 미리보기에서 `transport_error`로 규칙 fallback되어 DeepSeek 설명 포함 요구는 달성하지 못했다. 최종 미리보기 해시는 `a7b97ce633283d9e2414d60a79041ebdaa4acd35449de64ae7111ea874d0db02`, `sent=false`이며 실제 Telegram 전송은 수행하지 않았다. (2026-07-21, Codex)
- [완료] DeepSeek V4 Flash 호출 경계를 NVIDIA hosted NIM으로 전환했다. `NVIDIA_API_KEY`, `https://integrate.api.nvidia.com/v1/chat/completions`, `deepseek-ai/deepseek-v4-flash`, `reasoning_effort=none`, JSON object·비스트리밍·256토큰 계약을 코드·테스트·프로젝트/상위 Codex 설정·Claude 설정·세 Skill·문서에 반영했다. focused 68개와 전체 129개 테스트, Ruff lint·format, 세 Skill 검증·해시 일치, 비밀값·절대경로 검색을 통과했다. 기존 변수의 새 키를 일회성으로 `NVIDIA_API_KEY`에 매핑한 실호출도 HTTP 200, `interpretation.status=applied`, 참조 ID 일치, `sent=false`를 확인했다. 일반 MCP 적용을 위해 사용자는 Windows 변수 이름을 `NVIDIA_API_KEY`로 옮기고 앱을 재시작해야 한다. (2026-07-21, Codex)
- [중단] `fear-greed-report` Skill로 현재 보고서와 Telegram 미리보기를 생성했다. 7개 고유 지표·완전성·최신성·규칙 요약·Python 선정 요인 일치를 확인했지만, NVIDIA 키를 DeepSeek 공식 endpoint로 보낸 공급자 불일치 때문에 `http_401`로 규칙 fallback됐다. `sent=false`로 실제 Telegram 전송은 하지 않았으며, 위 NVIDIA hosted NIM 전환 작업이 후속 조치를 대체한다. (2026-07-21, Codex)
- [완료] DeepSeek 전환 계획의 5단계로 Codex·Claude Skill, 프로젝트·상위 Codex 및 Claude MCP 설정, README·설계·결정·에이전트 지침을 실제 동작과 비밀값 주입 방식에 맞췄다. 실제 키·토큰은 저장소 밖의 Windows 사용자 환경변수에 두고 설정 파일에는 변수 이름/`${VAR}` 참조만 기록한다. 전체 테스트 128개, Ruff, stdio 계약, 세 Skill 검증·해시 일치, 비밀키 패턴·개인 절대경로 검색 0건을 확인했다. 실제 외부 API와 Telegram 전송은 하지 않았다. (2026-07-21, Codex)
- [완료] DeepSeek 전환 계획의 4단계로 검증된 해석기를 `preview_telegram_report` 내부에만 연결했다. 성공 시 해석문을 Telegram 원문·SHA-256에 포함하고 모델명은 MCP 메타데이터에만 기록하며, 키 누락·전송·HTTP·응답·검증·예상 밖 공급자 오류는 비밀값 없는 사유 코드와 규칙 fallback으로 격리한다. `get_fear_greed_report`는 DeepSeek를 호출하지 않고 Telegram 실제 전송 계약도 변경하지 않았다. 전체 테스트 128개와 Ruff lint·format 검사를 통과했다. (2026-07-21, Codex)
- [완료] DeepSeek 전환 계획의 3단계로 Python 선정 요인만 직렬화하는 전용 프롬프트와 반환 JSON 파서·검증기를 구현했다. `lines`·`referenced_indicator_ids` 두 필드만 허용하고 ID 순서 일치, 1~2줄, 줄바꿈 포함 300자 이내, 뉴스·전망·매매 표현 금지를 mock completion으로 검증했다. 선택되지 않은 지표와 종합지수는 프롬프트에 포함하지 않으며, 전체 테스트 121개와 Ruff lint·format 검사를 통과했다. 아직 MCP 미리보기에는 연결하지 않았다. (2026-07-21, Codex)
- [완료] DeepSeek 전환 계획의 2단계로 `httpx` 기반 REST 클라이언트를 추가했다. `DEEPSEEK_API_KEY` 환경변수, `deepseek-v4-flash`, 비사고 모드, JSON object 응답, 256토큰 출력 한도와 설정·전송·HTTP·응답 오류 분류를 구현했다. 실제 외부 호출은 하지 않고 mock 테스트 15개를 포함한 전체 테스트 112개와 Ruff lint·format 검사를 통과했다. MCP 미리보기 연결과 최종 프롬프트·의미 검증은 아직 추가하지 않았다. (2026-07-21, Codex)
- [완료] 확정한 DeepSeek API 전환 계획의 1단계로, 클라이언트가 해석문·참조 지표·모델명·보고서 해시를 주입하던 MCP 공개 계약과 Codex `gpt-5.4-mini`·Claude Code `haiku` 모델 분기를 제거했다. `preview_telegram_report` 공개 입력은 `force_refresh`만 남겼고, 내부 해석 검증·렌더링 골격은 다음 DeepSeek 연결 단계에서 재사용하도록 보존했다. 전체 테스트 97개와 Ruff lint·format 검사를 통과했다. (2026-07-21, Codex)
- [중단] Codex `gpt-5.4-mini`·Claude Code `haiku` 서브에이전트를 이용하는 경량 설명 구현은 DeepSeek API를 직접 호출하는 방향으로 요구가 변경되어 부분 구현 상태에서 중단했다. 관련 테스트는 실행하지 않았으며, 다음 구현에서 공개 MCP 입력 계약과 모델 분기를 제거·교체한다. (2026-07-21, Codex)
- [완료] Python이 요인을 결정하고 `deepseek-v4-flash` 비사고 모드가 그 의미만 1~2줄·300자 이내로 해석하는 방향으로 계획을 수정했다. DeepSeek 호출은 Telegram 미리보기 내부에서만 수행하고, 키는 `DEEPSEEK_API_KEY` 환경변수로만 주입하며, 실패·형식 오류 시 기존 규칙 문장으로 fallback하고 모델명은 MCP 메타데이터에만 기록한다. 아직 운영 코드 구현은 재개하지 않았다. (2026-07-21, Codex)
- [완료] Python 요인 선정은 유지하고 Codex `gpt-5.4-mini`·Claude Code `haiku` 서브에이전트가 2줄 이내 의미 설명을 생성해 Telegram 미리보기·해시에 포함하도록 경량 변경 설계와 구현·테스트 계획을 수립했다. 자유 텍스트 대신 설명 줄 배열·참조 지표 ID·보고서 해시를 검증하고 실패 시 기존 규칙 문장으로 복귀하도록 설계했다. (2026-07-21, Codex)
- [완료] 규칙 기반 한 줄 해석 뒤에 실행 에이전트의 경량 모델 설명을 2줄 이내로 추가하려는 요구를 검토했다. 채팅에만 덧붙이면 Skill 변경으로 가능하지만 Telegram 미리보기·전송 원문에 포함하려면 플랫폼별 경량 서브에이전트와 MCP 미리보기 입력·검증·렌더링·해시 계약을 함께 확장해야 한다. (2026-07-21, Codex)
- [완료] `fear-greed-report` Skill로 현재 보고서를 조회하고 7개 지표·규칙 기반 요인·데이터 품질을 검증한 뒤 Telegram 미리보기까지 생성했다. 지표는 고유 ID 7개였고 데이터는 완전·최신, 요약 출처는 규칙이었으며 실제 전송은 수행하지 않았다. (2026-07-21, Codex)
- [완료] 로컬 stdio MCP 서버를 실제 클라이언트로 실행해 초기화·도구 목록과 CNN 보고서 조회·Telegram 미리보기 요청/응답 구조를 확인했다. 7개 지표·규칙 요인·데이터 품질·미리보기 해시가 모두 일치했고 실제 전송은 하지 않았다. (2026-07-20, Codex)
- [완료] Claude Code 설치 상태와 `.mcp.json`·Skill 실행 절차를 공식 문서로 검증했다. `${CLAUDE_PROJECT_DIR:-.}` 기본값 표기로 수정하고 Claude 설정 기반 stdio 초기화까지 확인했다. (2026-07-20, Codex)
- [완료] Codex 앱의 현재 작업 폴더가 상위 폴더라 하위 `.codex`·`.agents`가 탐색되지 않는 문제와 상대경로 오류를 수정했다. 상위 연결 설정과 Skill을 추가하고 Codex 목록에서 `fng_chatbot enabled`를 확인했다. (2026-07-20, Codex)
- [완료] 로컬 MCP 구현과 `fear-greed-report` Skill의 도구 계약·안전 절차를 읽고 정합성을 확인했다. (2026-07-20, Codex)
- [완료] 로컬 MCP·Codex/Claude Skill 구조의 최종 포맷, Skill, 비밀값, git diff 검증을 완료했다. (2026-07-20, Codex)
- [완료] Cloud·HTTP·Docker 경로와 Gemini·DeepSeek LLM 계층을 제거하고 프로젝트 이름을 `fng-chatbot`으로 정리했다. (2026-07-20, Codex)
- [완료] 규칙 기반 보고서 생성과 MCP 도구 3개를 연결하고 전체 테스트 `83 passed`를 확인했다. (2026-07-20, Codex)
- [완료] Codex/Claude용 MCP 설정과 동일한 `fear-greed-report` Skill을 자동 탐색 경로에 배치했다. (2026-07-20, Codex)

## 최근 체크포인트

- 2026-07-21 21:44 KST 무렵 Codex가 표시했던 미리보기 SHA-256 `8406f767c572c19c6baa9b8b766d5cfa15d4542083fb7b054924880bd0bca3c0` 원문을 사용자 승인에 따라 전송했다. 전송 전 직접 재구성 문자열과 새 미리보기는 해시 불일치로 중단했고, UTF-8 파일에서 복원한 원문이 승인 해시와 일치할 때만 `send_telegram_report`를 호출했다. 전송 결과는 `message_id=320`, `idempotency_key=daily-report:2026-07-21`, `sent=true`다. 다음 시작 지점은 없음 — 이번 요청은 정상 종료됐다.
- 2026-07-21 Claude Code 세션에서 `fear-greed-report` Skill 전체 절차(조회→품질 검증→미리보기→승인 전송)를 완주했다. CNN F&G 종합은 36.94점(공포), 7개 지표는 고유·완전·최신이며 Python 선정 공포 요인은 시장 폭(15.0, extreme fear)·풋·콜 옵션(32.0, fear), 완충 요인은 시장 변동성(50.0, neutral)이다. DeepSeek 해석은 `status=applied`, `source=deepseek`, 참조 ID 순서 `stock_price_breadth`, `put_call_options`, `market_volatility`로 Python 선정 순서와 일치했다. 사용자가 표시된 미리보기를 수정 없이 전송 승인해 SHA-256 `7609d4f2bc61252694bc658844c8b2a6337acfc565f4aedb9be73ee5a66af0f3` 원문 그대로 `send_telegram_report`를 호출했고, `message_id=319`, `idempotency_key=daily-report:2026-07-21`로 실제 Telegram 전송에 성공했다. 다음 시작 지점은 없음 — 이번 요청은 정상 종료됐다.
- 2026-07-21 21:37 KST 무렵 `fear-greed-report` Skill 실행 결과, CNN F&G는 36.94점(공포)이고 7개 지표는 고유·완전·최신이다. Python 선정 공포 요인은 시장 폭(15.0, extreme fear)과 풋·콜 옵션(32.0, fear), 완충 요인은 시장 변동성(50.0, neutral)이며 DeepSeek 설명은 `status=applied`, `source=deepseek`로 적용됐다. 참조 ID는 `stock_price_breadth`, `put_call_options`, `market_volatility` 순서로 Python 선정 요인과 일치한다. Telegram 미리보기 SHA-256은 `8406f767c572c19c6baa9b8b766d5cfa15d4542083fb7b054924880bd0bca3c0`, `sent=false`이고 실제 전송은 하지 않았다. 다음 시작 지점은 사용자가 이 정확한 미리보기 전송을 명시 승인할 경우 동일 원문·해시로 `send_telegram_report`를 호출하는 것이다.
- 금지 표현 검증 제거를 완료하고 새 프로세스에서 DeepSeek 설명 적용을 처음으로 확인했다. `interpretation.py`에서 `_FORBIDDEN_PHRASES`, 검증 블록, `validation_forbidden_language` 사유 코드를 제거했고 남은 검증 코드는 11개다. 시스템 프롬프트의 금지 지시와 `test_report.py`의 규칙 출력 검사는 유지했다. 전체 168개 테스트와 Ruff lint·format을 통과했다. 실검증 결과는 `status=applied`, `source=deepseek`, 설명 2줄, 참조 ID 순서 `stock_price_breadth`·`put_call_options`·`market_volatility` 일치, 미리보기 886자, SHA-256 `6c4fba8c2051b2b381ae0ca910df6a1cbe9f78bc6b06ccb1210a3758b93bd3a0`, `sent=false`였다.
- **주의**: 실행 중인 stdio MCP 서버는 세션 시작 시점의 코드를 메모리에 유지하므로 `src` 변경 후 MCP 도구를 호출하면 옛 동작이 그대로 나온다. 실제로 제거 직후 MCP 미리보기가 계속 `validation_forbidden_language`를 반환했고, 새 Python 프로세스에서만 `status=applied`가 확인됐다. 다음 시작 지점은 Codex·Claude 앱을 재시작해 일반 MCP 경로에서 `status=applied`를 재확인하는 것이다.
- 매수·매도 권유를 막는 강제 장치는 이제 없다. 프롬프트 지시와 전송 전 사용자 승인·SHA-256 결속만 남으므로 미리보기 문구를 육안으로 확인한 뒤 승인해야 한다.
- DeepSeek 재시도 수정을 완료하고 실검증했다. `deepseek.py`는 이제 재시도 사이에 1·2·4초 지수 backoff와 25% jitter를 두고 최대 4회 시도하며, 누적 45초 예산을 넘길 재시도는 스스로 포기해 MCP 90초 예산을 보호한다. HTTP 거절 본문은 여섯 개 고정 어휘로 요약되어 `mcp_server.py`가 `http_503_resource_exhausted` 형태의 사유를 반환하고, 어휘 밖 값과 파싱 불가 본문은 `unspecified`로 축소되어 기존 `http_503` 형태를 유지한다. 전체 170개 테스트(신규 12개 포함)와 Ruff lint·format을 통과했고, 재시도 테스트는 `sleep`을 주입해 실제 대기 없이 6초 안에 끝난다. 실 endpoint 5회 검증은 4회 성공(최장 9.86초)이었고 1회 실패는 새 코드 `http_503_resource_exhausted`로 정확히 분류됐다. Telegram 전송과 미리보기 생성은 수행하지 않았다.
- `http_503`의 실원인은 공급자 설정이 아니라 NVIDIA 공용 워커 풀 포화이며, 실패를 확정적으로 만든 것은 대기 없는 재시도였다. 실측 근거: 앱과 동일한 payload가 HTTP 200을 반환했고, `NVIDIA_API_KEY`는 형식·길이 모두 정상, `deepseek-ai/deepseek-v4-flash`는 카탈로그에 존재하며, 503 본문은 `ResourceExhausted: Worker local total request limit reached (48/48)`였다. 성공률은 무대기 2회 5/6, backoff 4회 6/6이었다. `reasoning_effort`·`response_format`은 원인이 아니다(제거한 요청이 오히려 503).
- `http_503` 진단상 20초 timeout 증가는 해결책이 아니다. 서버가 HTTP 응답으로 503을 즉시 반환했으므로 네트워크 대기 문제가 아니라 hosted inference 가용성 문제다. `deepseek.py`는 429·5xx를 재시도 대상으로 분류하지만 재시도 사이 대기와 응답 `Retry-After` 처리가 없다. 다음 구현 시작 지점은 응답 헤더를 읽을 수 있도록 HTTP 응답 계약을 확장하고, 503/429에 `Retry-After` 또는 2초·4초 지수 backoff+jitter를 적용해 최대 3회만 시도하며 전체 MCP 90초 예산과 규칙 fallback을 보존하는 것이다.
- 2026-07-21 20:56 KST 무렵 CNN F&G는 36.94점(공포)이다. 7개 지표는 고유·완전·최신이고 `summary_source=rules`이며, Python 선정 공포 요인은 시장 폭(15.0)·풋·콜 옵션(32.0), 완충 요인은 시장 변동성(50.0)으로 원 지표와 일치했다. NVIDIA hosted DeepSeek는 세 미리보기 모두 제한 재시도까지 HTTP 503을 반환해 최종 `status=rules_fallback`, `reason=http_503`이며, Telegram 미리보기 SHA-256은 `ec95cc7f1e15c356f011ab74a344cc26a79731c8219c213f94351b8c87d7ad50`, `sent=false`다. 다음 시작 지점은 공급자 503 해소 후 새 미리보기를 생성해 `status=applied`와 참조 ID 순서 `stock_price_breadth`, `put_call_options`, `market_volatility`를 확인하는 것이다.
- 최종 검증 결과는 중간 위험 변경에 대해 GO다. 전체 `pytest -q`는 150 passed, Ruff lint는 통과, format은 26 files already formatted, 프로젝트 Codex·Claude Skill과 상위 Codex Skill 본문은 일치, 실제 비밀값 패턴·개인 절대경로는 0건, scoped `git diff --check` 오류는 없었다. 전송 기본 비활성·명시적 승인·원문 SHA-256 계약은 전체 회귀 테스트에서 유지됐고 실호출도 `sent=false`였다. 운영 반영 후 `transport_*`, `http_429`·`http_5xx`, `validation_*` fallback 비율이 증가하거나 DeepSeek 설명 적용이 연속 실패하면 변경된 timeout·재시도·세부 코드 부분을 되돌리는 것이 rollback 기준이다.
- 429·5xx 제한 재시도까지 보완한 focused 54개 테스트와 Ruff 검사가 통과했고 실 MCP 미리보기가 최종 성공했다. NVIDIA endpoint는 HTTP 200, `interpretation.status=applied`, 설명 2줄, 참조 ID 순서 `stock_price_breadth`, `put_call_options`, `market_volatility`를 반환했다. Telegram 미리보기는 919자, SHA-256 `76b3825f2e23ec8beb797c08a4eb293404f51a43f74266273cdc5363401636f1`, `sent=false`였다. 다음 시작 지점은 전체 pytest·Ruff, 두 Skill·두 MCP 설정, 비밀값·절대경로, 전송 비활성 계약을 최종 검증하는 것이다.
- 20초 timeout과 read-timeout 1회 재시도를 적용한 뒤 focused 50개 테스트와 Ruff 검사를 통과했고 실미리보기를 재검증했다. 이번 DeepSeek 요청은 timeout이 아니라 NVIDIA endpoint의 HTTP 503으로 즉시 분류되어 규칙 fallback됐으며, Telegram 미리보기 SHA-256은 `a6ce8a77067d83e6707a79e872b23e00b91a119e480604e92249e7daf8067747`, `sent=false`였다. 다음 시작 지점은 미리보기에서 일시적 429·5xx에만 총 2회 제한 재시도를 적용하고 401/403 등 비재시도 오류 계약을 보존한 뒤 다시 실검증하는 것이다.
- 세부 코드 적용 후 실 MCP 조회→검증→미리보기를 실행했다. CNN 보고서는 7개 고유 지표·완전·최신·규칙 요약·선정 요인 일치를 통과했고, DeepSeek 실패가 `transport_read_timeout`임을 특정했다. Telegram 미리보기는 규칙 fallback, SHA-256 `44df4ac52d6d74f5c3d10d32fb1fd8dd240c1510a6ed68da5875a9b772003cba`, `sent=false`였다. 다음 시작 지점은 DeepSeek 기본 read timeout을 20초로 늘리고 read timeout에만 1회 제한 재시도를 추가한 뒤 실미리보기를 다시 검증하는 것이다.
- 우선순위 3단계를 완료했다. MCP fallback이 `DeepSeekTransportError.reason_code`와 `InterpretationValidationError.reason_code`를 그대로 반환하되 예외 메시지·원시 응답은 노출하지 않는다. 세부 코드와 기존 일반 코드의 호환 계약을 보강했고 관련 67개 테스트와 6개 파일 Ruff lint·format 검사가 통과했다. 다음 시작 지점은 전송을 비활성화한 실미리보기에서 새 `interpretation.reason`을 관측해 필요한 조정만 수행하는 것이다.
- 우선순위 2단계를 완료했다. `DeepSeekTransportError`가 connect/read/write/pool timeout과 connect/read/write/close/proxy/network/기타 요청 오류를 고정된 비밀값 없는 사유 코드로 보존하며, 알 수 없는 코드는 `transport_error`로 축소한다. `tests/test_deepseek.py` 27개와 해당 Ruff lint·format 검사가 통과했다. timeout은 진단 근거를 얻기 전이므로 기존 10초를 유지했다. 다음 시작 지점은 이 세부 코드를 MCP fallback 메타데이터에 연결하는 3단계다.
- 우선순위 1단계를 완료했다. `InterpretationValidationError`가 invalid JSON, 비객체, 키 집합, 배열 타입, 줄 수·형태·길이, 참조 ID 불일치, 모델 누락, 금지 표현을 각각 고정된 비밀값 없는 사유 코드로 보존하며, 알 수 없는 코드는 `validation_error`로 축소한다. `tests/test_interpretation.py` 20개와 해당 Ruff lint·format 검사가 통과했다. 다음 시작 지점은 `httpx.RequestError` 하위 타입을 안전한 transport 코드로 분류하는 2단계다.
- DeepSeek 실패의 우선 수정 대상은 환경변수가 아니라 코드의 오류 분류·진단 경계다. 첫 요청의 HTTP 200은 키·endpoint·인증이 정상임을 입증하며, 키 누락은 `configuration_error`, 잘못된 키는 HTTP 401/403으로 나타나므로 이번 `validation_error`·`transport_error`와 다르다. 다음 시작 지점은 `interpretation.py`에 JSON/스키마/줄/길이/ID/금지어별 안전한 검증 코드를, `deepseek.py`에 connect/read/write/timeout별 안전한 transport 코드를 추가하고, `mcp_server.py`가 그 코드를 메타데이터로 반환하도록 테스트와 함께 수정하는 것이다. 그 진단 결과가 read timeout일 때만 현재 10초 timeout과 제한 재시도를 조정한다.
- 직전 DeepSeek 미리보기의 첫 시도는 `POST https://integrate.api.nvidia.com/v1/chat/completions`가 HTTP 200을 반환했으므로 API 호출 자체는 성공했다. 실패 지점은 그 뒤의 로컬 `parse_deepseek_interpretation`/`validate_interpretation`이며, 후속 두 시도는 HTTP 응답 완료 전 `transport_error`였다. 현재 안전 메타데이터만으로는 첫 응답이 정확히 어느 검증 규칙을 위반했는지, 후속 오류가 연결·읽기·쓰기·타임아웃 중 무엇인지 확정할 수 없다. 다음 시작 지점은 필요 시 비밀값과 원문을 남기지 않는 진단 코드로 검증 단계명과 transport 하위 분류만 관측 가능하게 개선하는 것이다.
- 2026-07-21 19:52 KST 기준 CNN F&G는 36.94점(공포)이다. 7개 지표는 고유·완전·최신이고 `summary_source=rules`이며, Python 선정 공포 요인은 시장 폭(15.0)·풋·콜 옵션(32.0), 완충 요인은 시장 변동성(50.0)으로 원 지표와 일치했다. DeepSeek는 첫 응답의 검증 실패 후 두 차례 전송 오류로 최종 `status=rules_fallback`, `reason=transport_error`였고, Telegram 미리보기 SHA-256은 `a7b97ce633283d9e2414d60a79041ebdaa4acd35449de64ae7111ea874d0db02`, `sent=false`다. 다음 시작 지점은 NVIDIA endpoint 연결 상태와 출력 검증 실패 원인을 점검한 뒤 새 미리보기를 생성해 `status=applied`와 참조 ID 순서 `stock_price_breadth`, `put_call_options`, `market_volatility`를 확인하는 것이다.
- NVIDIA hosted DeepSeek 전환을 완료했다. 실제 미리보기 호출은 `deepseek-ai/deepseek-v4-flash`에서 HTTP 200과 `status=applied`를 반환했고, 참조 ID는 Python 선정 순서 `stock_price_breadth`, `put_call_options`, `market_volatility`와 일치했다. Telegram 미리보기는 956자, SHA-256 `3b6981a67b0427b147e99d454e71371b97ba11d4dec927456976c12488da1fbf`, `sent=false`였다. 현재 새 키 값은 프로세스의 기존 `DEEPSEEK_API_KEY` 이름에 있어 실호출에서만 임시 매핑했으므로, 다음 시작 지점은 Windows 사용자 변수 `NVIDIA_API_KEY`로 옮기고 `MCP_ALLOW_TELEGRAM_SEND=false`를 확인한 뒤 Codex 앱을 재시작해 일반 MCP 경로로 미리보기를 한 번 재검증하는 것이다.
- 2026-07-21 현재 CNN F&G는 36.94점(공포)이며, 7개 지표는 고유·완전·최신이고 Python 선정 공포 요인은 시장 폭(15.0)·풋·콜 옵션(32.0), 완충 요인은 시장 변동성(50.0)으로 원 지표와 일치했다. DeepSeek 요청은 `http_401`로 규칙 fallback됐고, Telegram 미리보기 해시는 `a2473e7526a89f2fa71722b016133dfb8556d9e6215729c2974b1982541de68b`, `sent=false`다. 다음 시작 지점은 유효한 키 재설정과 앱 재시작 후 같은 미리보기를 다시 생성해 `interpretation.status=applied`와 참조 ID 순서를 검증하는 것이다.
- DeepSeek 전환 5단계와 전체 계획을 완료했다. 프로젝트·상위 Codex 설정은 `DEEPSEEK_API_KEY`를 포함한 환경변수 이름만 MCP로 전달하고, Claude `.mcp.json`은 `${VAR}` 확장과 안전한 기본값을 사용한다. README에 Windows 사용자 환경변수 위치, 앱 재시작 시점, DeepSeek 미리보기 연결과 Telegram 전송 잠금 해제 절차를 기록했다. 실제 비밀값 설정과 유료 API/Telegram 실호출은 사용자가 별도로 수행할 운영 단계로 남겼다.
- DeepSeek 전환 4단계를 완료했다. 기본 MCP 서비스가 `DeepSeekInterpreter(DeepSeekClient())`를 보유하되 호출은 Telegram 미리보기에서만 수행한다. 성공 메타데이터는 `status=applied`, `source=deepseek`, 모델·해석 줄·참조 ID를 담고, 실패 메타데이터는 `status=rules_fallback`, `source=rules`, `model=null`과 정규화된 사유 코드만 담는다. 모델명과 오류 원문은 Telegram 본문에 들어가지 않는다. 다음 시작 지점은 두 Skill·두 MCP 설정·설계/결정/사용 문서를 새 동작과 환경변수에 맞추고 최종 보안·계약 검증을 수행하는 5단계다.
- DeepSeek 전환 3단계를 완료했다. `interpretation.py`가 Python 선정 요인만 JSON 프롬프트로 만들고 DeepSeek 응답의 정확한 키·문자열 배열·참조 ID·1~2줄·줄바꿈 포함 300자·금지 표현을 검증해 `source=deepseek` 해석으로 변환한다. 다음 시작 지점은 이를 `preview_telegram_report` 내부에 연결하고 모든 오류에서 규칙 fallback과 안전한 MCP 메타데이터를 반환하는 4단계다.
- DeepSeek 전환 2단계를 완료했다. `src/fng_chatbot/deepseek.py`가 환경변수 키를 읽어 공식 `/chat/completions`에 `deepseek-v4-flash`·비사고 모드·JSON object·비스트리밍·256토큰으로 요청하며, 비밀값을 노출하지 않는 오류 경계를 제공한다. `.env.example`에는 키 이름과 명백한 placeholder만 추가했다. 다음 시작 지점은 Python 선정 요인만 전달하는 전용 프롬프트와 반환 JSON 의미 검증을 구현하는 3단계다.
- DeepSeek 전환 1단계를 완료했다. 외부 에이전트가 해석문을 주입하던 MCP 인자 4개와 공개 `interpretation_context`/보고서 해시, Codex·Claude 모델 허용 분기를 제거했다. 미리보기는 DeepSeek가 아직 연결되지 않은 현재 단계에서 규칙 fallback 메타데이터를 반환한다. 다음 시작 지점은 `httpx` 기반 DeepSeek REST 클라이언트와 `DEEPSEEK_API_KEY` 환경변수 구성을 추가하는 2단계다.
- 최신 계획은 Codex/Claude Code 서브에이전트가 아니라 Python MCP 서비스가 DeepSeek REST API를 직접 호출하는 구조다. Python 규칙이 선택한 공포 요인 최대 2개와 완충 요인 최대 1개만 점수·등급·근거와 함께 전달하고, DeepSeek는 JSON 형식의 1~2줄 의미 설명과 참조 지표 ID만 반환한다. Python이 JSON·참조 ID·길이·금지 표현을 검증한 뒤 Telegram 미리보기와 SHA-256에 포함한다.
- DeepSeek 호출은 `preview_telegram_report`에서만 수행하고 `get_fear_greed_report`는 비용과 외부 의존성 없이 결정적으로 유지한다. 모델은 현재 공식 경량 모델 `deepseek-v4-flash`, 사고 모드는 비활성화한다. 키 누락·전송 오류·429/5xx·잘못된 JSON·검증 실패 시 Telegram 생성 자체를 실패시키지 않고 규칙 전용 문장으로 fallback하며, 모델명은 Telegram 본문이 아닌 MCP 메타데이터에만 기록한다.
- Codex/Claude용으로 시작한 부분 변경 파일은 `src/fng_chatbot/interpretation.py`, `src/fng_chatbot/mcp_server.py`, `src/fng_chatbot/message_renderer.py`, `src/fng_chatbot/telegram.py`, `tests/test_interpretation.py`, `tests/test_mcp_tools.py`, `tests/test_message_renderer.py`다. 다음 구현은 이를 무조건 이어 쓰지 않고, 재사용 가능한 출력 검증·렌더링 부분만 보존하며 클라이언트가 설명을 넘기는 공개 입력 계약과 `gpt-5.4-mini`/`haiku` 분기를 제거하는 지점부터 시작한다.
- 2026-07-21 현재 CNN F&G는 36.94점(공포)이다. 고유 지표 7개, `complete=true`, 누락 없음, `stale=false`, `summary_source=rules`와 공포 요인·완충 요인의 원 지표 일치를 확인했다. Telegram 미리보기는 775자, SHA-256 `107e957d754e280fd1e42140a55ffa138e0bb7ceeaf35c5ab34926ab23ddaec6`, `sent=false`이며 실제 전송하지 않았다.
- 로컬 stdio MCP를 실제로 시작해 프로토콜 `2025-11-25`, 서버 `F&G Market Sentiment`와 도구 3개를 확인했다. 실시간 CNN 조회와 시장시세 미리보기가 성공했고, 보고서는 7개 고유 지표·완전/최신 데이터·`summary_source=rules`를 반환했다. Telegram 미리보기는 784자, SHA-256 일치, `sent=false`였으며 전송 환경변수도 false로 유지했다. stdio 서버는 검증 클라이언트 종료와 함께 정상 종료됐다.
- MCP 도구 3개와 Codex·Claude Skill의 조회→검증→미리보기→승인 전송 계약을 확인했고, 관련 계약 테스트는 `16 passed`다.
- 제거 전 구현은 복구 기준점 커밋 `d2cf7ac`에 보존했다.
- 현재 핵심 파일은 `src/fng_chatbot/`, `.codex/config.toml`, `.mcp.json`, `.agents/skills/`, `.claude/skills/`, `AGENTS.md`, `CLAUDE.md`다.
- `pytest -q`: `83 passed`; Ruff lint·format: 통과; Codex·Claude Skill 검증: 통과.
- 자동 인식 수정 후 `pytest -q`: `85 passed`; 상위·하위 MCP stdio 초기화와 두 Skill 검증, Codex `mcp list`의 `fng_chatbot enabled`를 확인했다.
- Claude 설정 수정 후 `pytest -q`: `86 passed`; `.mcp.json` JSON 검증, Claude 명령 경로 확장, MCP 도구 3개 초기화를 확인했다. 이 PC에는 Claude Code가 아직 설치되어 있지 않다.
- 생성형 AI·Cloud 실행 흔적, 비밀키 패턴, 공개 제외 문구 검사: 검출 없음.
- 실제 Telegram 전송과 외부 예약 실행은 수행하지 않았다.

## 주의 / 막힌 것

- CNN 주소는 공식 개발자 API가 아니므로 필드 누락·차단·타임아웃을 정상 오류 경로로 처리한다.
- 실제 API 키와 Telegram 토큰은 저장소에 저장하지 않는다.
- 자동 매매 신호와 매수·매도 권유는 범위 밖이다.
- Telegram 전송은 기본 비활성화이며, 사용자가 미리보기를 검수한 뒤에만 별도로 활성화한다.
