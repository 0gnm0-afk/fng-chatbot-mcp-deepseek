# fng-chatbot 설계

## 1. 목표

하나의 로컬 Python 코어를 stdio MCP로 노출하고, Codex와 Claude Code가 각자의 프로젝트 Skill을 통해 같은 안전한 분석 절차를 실행하게 한다.

핵심 원칙은 다음과 같다.

- CNN 원본 응답과 내부 정규화 모델을 분리한다.
- 비교 기준 필드를 새 지표로 이중 계산하지 않고 정확히 7개 개념 지표만 제공한다.
- 공포·완충 요인과 요약은 Python 규칙으로 재현 가능하게 만든다.
- 부가 시세 장애가 핵심 F&G 사실 보고서를 막지 않게 한다.
- 명시적 미리보기 전용 경로와 기본 즉시 전송 경로를 분리하고, 즉시 전송은 내부에서 한 번 렌더링·해시한 원문만 보낸다.
- MCP는 로컬 stdio만 사용한다.

## 2. 구성요소

| 모듈 | 책임 |
|---|---|
| `cnn_fng_client.py` | CNN 요청, 오류 분류, 제한 재시도, 짧은 캐시 |
| `normalizer.py` | 원본 JSON을 종합점수와 7개 지표로 변환 |
| `models.py` | 정규화 데이터와 품질 계약 |
| `market_data.py` | 코스피·환율·비트코인·미국 현물·선물 시세 수집과 실패 격리 |
| `driver_selector.py` | 결정적 공포·완충 요인 선택 |
| `report.py` | 정규화 사실을 정보성 요약으로 변환 |
| `deepseek.py` | NVIDIA hosted DeepSeek 고정 모델의 REST 인증·요청·응답 오류 경계 |
| `interpretation.py` | Python 선정 요인 전용 프롬프트·JSON 검증·fallback 메타데이터 |
| `message_renderer.py` | 시장시세와 F&G 보고서 결합 |
| `telegram.py` | MarkdownV2 렌더링과 단일 전송 |
| `mcp_server.py` | 코어 서비스를 MCP 도구 3개로 노출 |

## 3. 데이터 계약

정규화 결과는 다음을 포함한다.

- `overall`: 현재 종합점수, 등급, 전일·전주·전월·전년 비교
- `indicators`: 서로 다른 ID를 가진 정확히 7개 지표
- `data_quality`: 완전성, 누락 필드, 캐시, stale 여부
- `source`, `fetched_at`

응답에 없는 값은 추정해 채우지 않는다. 필수 필드가 없으면 설명 가능한 스키마 오류로 처리한다.

## 4. 규칙 기반 요인과 DeepSeek 보조 설명

Python은 정규화된 7개 지표에서 다음 순서로 보고서를 만든다.

- 공포·극단적 공포 등급 중 점수가 낮은 순서로 최대 2개 선택
- 중립·탐욕·극단적 탐욕 등급 중 점수가 높은 지표를 완충 요인으로 최대 1개 선택
- 동점이면 고정된 7개 지표 순서로 결정
- 선택한 지표의 ID·명칭·점수·등급을 그대로 복사해 규칙 문장 생성
- 매매 권유나 가격 전망 없이 종합점수와 선택 결과만 요약

`get_fear_greed_report`는 여기까지 수행하므로 같은 입력에 항상 같은 결과를 만들며 외부 모델을 호출하지 않는다.

`preview_telegram_report`와 내부적으로 같은 생성 경로를 쓰는 `send_telegram_report`는 Python이 이미 선택한 공포 요인 최대 2개와 완충 요인 최대 1개의 역할·ID·명칭·점수·등급·근거만 NVIDIA hosted `deepseek-ai/deepseek-v4-flash`에 전달한다. 호출은 `https://integrate.api.nvidia.com/v1/chat/completions`에서 `reasoning_effort=none`, JSON object, 비스트리밍, 최대 256토큰, 요청별 20초 timeout으로 고정한다. read timeout과 일시적인 HTTP 429·5xx에만 총 4회까지 제한 재시도하되, 재시도 사이에 1·2·4초 지수 backoff와 25% jitter를 두고 전체 45초 예산을 넘기면 남은 시도를 포기한다. 연결·쓰기 오류와 401·403 같은 비재시도 HTTP 오류는 즉시 fallback한다. DeepSeek는 요인을 다시 선택하지 않고 그 의미만 JSON의 1~2줄, 줄바꿈 포함 300자 이내로 설명한다. Python은 정확한 필드, 참조 ID와 순서, 줄 수, 길이를 다시 검증한다. 표현 자체를 차단하는 금지 어휘 검증은 두지 않고, 매매 권유·전망 배제는 시스템 프롬프트 지시로만 요구한다.

키 누락, 통신·HTTP·응답·검증 오류 또는 예상 밖 공급자 오류는 핵심 미리보기를 실패시키지 않는다. 규칙 문장만 사용하고 MCP `interpretation` 메타데이터에는 검증 단계와 transport 하위 유형을 구분하는 고정된 비밀값 없는 사유 코드만 기록한다. HTTP 거절은 공급자 응답 본문을 `resource_exhausted`·`rate_limited`·`model_unavailable`·`unauthorized`·`invalid_request`·`unspecified` 여섯 개 고정 어휘로만 요약해 `http_503_resource_exhausted` 형태로 남기므로, 용량 포화와 설정 오류를 구분할 수 있다. 알 수 없는 세부값은 일반 코드로 축소하고 모델명·오류 원문·모델 원문은 Telegram 본문이나 로그에 넣지 않는다.

## 5. 시장시세 경계

코스피는 네이버 응답을 우선 사용하고 실패하면 Yahoo `^KS11`로 대체한다. 나머지 Yahoo 심볼은 각각 독립적으로 검증한다.

한 항목의 장애는 다른 시세나 핵심 F&G 보고서를 숨기지 않는다. 누락 항목과 원인은 `data_quality`에 남긴다.

## 6. MCP 계약

MCP 서버 이름은 `F&G Market Sentiment`이고 로컬 stdio로 실행한다.

### 조회

`get_fear_greed_report`는 읽기 전용이며 구조화된 사실과 검증된 요약을 반환한다.

### 미리보기

`preview_telegram_report`는 읽기 전용이다. 환경에 키가 있으면 DeepSeek를 호출하고 일시 장애에만 한 번 더 시도할 수 있지만 Telegram Bot API는 호출하지 않는다. 검증된 설명 또는 규칙 fallback을 포함한 Telegram 원문과 SHA-256, `interpretation` 메타데이터를 반환한다.

### 전송

`send_telegram_report`는 현재 보고서·시장시세·DeepSeek 설명을 내부에서 한 번 렌더링한 뒤 다음 조건을 모두 확인한다.

1. 서버의 전송 허용 환경변수가 true(기본값 true, 명시적 false는 kill switch)
2. 내부 생성 원문의 SHA-256이 내부 미리보기 해시와 일치
3. 생성한 `TelegramPreview`와 실제 전송 원문이 동일

timeout 뒤에는 수신 여부가 불명확하므로 자동 재시도하지 않는다.

## 7. Skill과 에이전트 진입점

- Codex Skill: `.agents/skills/fear-greed-report/`
- Claude Code Skill: `.claude/skills/fear-greed-report/`
- Codex MCP 설정: `.codex/config.toml`
- Claude Code MCP 설정: `.mcp.json`

두 Skill 본문은 동일하게 유지한다. Skill은 조회 → 품질·근거 검수 → 기본 즉시 전송 순서를 사용하고, 사용자가 명시한 경우에만 미리보기 전용 경로를 사용한다. 저장소 규칙과 테스트 명령은 `AGENTS.md`와 `CLAUDE.md`에 둔다.

## 8. 비밀값과 안전

- 키·토큰·채팅 ID를 코드, fixture, 문서, 로그, 설정 파일에 저장하지 않는다.
- `.env.example`에는 변수 이름과 명백한 placeholder만 둔다.
- 실제 `NVIDIA_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`는 저장소 밖의 사용자 환경변수에 둔다. Codex 설정은 이름만 전달하고 Claude 설정은 `${VAR}` 참조만 저장한다. 기존 `DEEPSEEK_API_KEY`는 NVIDIA 호출에 사용하지 않는다.
- 실제 외부 호출과 Telegram은 단위 테스트에서 mock 또는 fixture로 대체한다.
- DeepSeek 키가 없으면 미리보기는 정상적으로 규칙 fallback한다.
- 전송 기본값은 true다. `MCP_ALLOW_TELEGRAM_SEND=false`는 운영 kill switch로 유지한다.
- 자동 매매와 투자 권유 기능을 추가하지 않는다.

## 9. 검증 기준

- 전체 pytest 통과
- Ruff lint와 format check 통과
- Codex Skill `quick_validate.py` 통과
- Codex·Claude Skill 본문 일치
- 두 MCP 설정이 같은 Python 모듈을 실행
- MCP client가 도구 3개를 발견하고 정적 보고서를 호출
- 조회 도구가 DeepSeek를 호출하지 않고, 미리보기 성공·fallback mock 계약이 통과
- 전송 기본 경로가 별도 승인 인자 없이 내부 미리보기와 같은 원문을 전송
- `MCP_ALLOW_TELEGRAM_SEND=false` 상태에서 실제 전송 차단
- 비밀값·개인 로컬 절대 경로 검색 0건
