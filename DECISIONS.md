# DECISIONS — F&G Market Sentiment MCP

## 2026-07-20 — 외부 MCP를 의존하지 않고 자체 CNN 클라이언트와 MCP를 만든다

- **결정**: `ycjcl868/mcp-server-fear-greed`의 데이터 구조와 브라우저형 HTTP 요청 헤더 전략을 참고하되, 프로젝트 내부에 `cnn_fng_client.py`를 작성해 CNN 응답을 직접 검증·정규화한다.
- **이유**: 과제의 학습 범위를 데이터 수집부터 자체 MCP 설계까지 포함하고, 외부 패키지 변경과 공급망 의존을 줄이기 위해서다.
- **버린 대안**: 공개 MCP 패키지를 그대로 설치해 중계 — 자체 설계와 오류 처리 역량을 보여주지 못한다.

## 2026-07-20 — 9개처럼 보이는 원본 필드를 7개 개념 지표로 합친다

- **결정**: S&P 500/125일 평균과 VIX/50일 평균 필드는 각각 시장 모멘텀과 시장 변동성의 비교 쌍으로 취급하고 별도 가중치로 세지 않는다.
- **이유**: CNN 방법론의 구성요소는 7개이며 비교 기준 필드를 별도 신호로 세면 같은 정보를 이중 반영하게 된다.
- **버린 대안**: 참고 저장소의 최상위 필드를 모두 독립 지표로 노출 — 사용자와 AI가 7개 구성요소를 오해할 수 있다.

## 2026-07-20 — 지표 선정은 Python, 문장화는 LLM이 담당한다

- **결정**: Python이 실제 점수·등급으로 공포 후보 최대 2개와 완충 후보 최대 1개를 선정하고, LLM은 주어진 후보를 설명만 한다.
- **이유**: 동일 입력의 재현성을 높이고 AI가 존재하지 않는 원인이나 중요도를 만들어내는 것을 막기 위해서다.
- **버린 대안**: 7개 지표를 통째로 주고 LLM이 중요 지표를 자유 선택 — 결과가 흔들리고 근거 검증이 어려워진다.

## 2026-07-20 — Gemini 기본, DeepSeek 교체 가능, 정적 fallback 필수

- **결정**: 저비용 Gemini 모델을 기본값으로 두되 공통 인터페이스로 DeepSeek를 선택할 수 있게 하고, 외부 AI 없이도 동작하는 `TemplateProvider`를 둔다.
- **이유**: 하루 한 번의 짧은 요약에는 고가 모델이 필요 없으며, 특정 공급자 장애·가격·정책 변화가 전체 알림을 막지 않게 하기 위해서다.
- **버린 대안**: Claude API에 고정 — 과제 기능 대비 비용·공급자 종속이 불필요하다.

## 2026-07-20 — 정보성 시장심리 진단만 제공한다

- **결정**: AI 결과는 주요 공포 요인, 완충 요인, 한 줄 해석으로 제한하고 매수·매도·종목 추천·수익률 예측을 금지한다.
- **이유**: CNN 지표는 심리 측정 자료이며 자동 거래 판단으로 확대하면 근거와 안전 범위를 벗어난다.
- **버린 대안**: STRONG_BUY/SELL 같은 자동 매매 신호 — 과제 범위와 안전 기준에 맞지 않는다.

## 2026-07-20 — Google Cloud Run 기준으로 처음부터 설계한다

- **결정**: 애플리케이션을 무상태 Linux 컨테이너로 만들고 `0.0.0.0:$PORT`에서 실행한다. 일일 실행은 인증된 Cloud Scheduler, 비밀값은 Secret Manager를 사용한다.
- **이유**: 기존 HTTP 챗봇과 MCP Streamable HTTP를 한 배포 단위로 운영할 수 있고, 사용량이 적을 때 scale-to-zero가 가능한 구조에 맞추기 위해서다.
- **버린 대안**: 로컬 장기 실행 프로세스나 특정 PC의 파일 저장에 의존 — Google Cloud 운영과 장애 복구에 맞지 않는다.

## 2026-07-20 — CNN 캐시는 성능 보조일 뿐 진실의 원천으로 삼지 않는다

- **결정**: 인스턴스 메모리에 짧은 TTL 캐시를 두되 Cloud Run 재시작과 다중 인스턴스에서도 캐시 없이 정상 동작하게 한다.
- **이유**: CNN 반복 호출과 차단 가능성을 낮추면서도 Cloud Run의 휘발성·수평 확장을 위반하지 않기 위해서다.
- **버린 대안**: 로컬 파일 캐시를 영속 저장소처럼 사용 — scale-to-zero와 인스턴스 교체 시 신뢰할 수 없다.

## 2026-07-20 — AI가 7개 지표 전체에서 요인을 직접 선정한다

- **결정**: 이 결정은 앞선 `지표 선정은 Python, 문장화는 LLM이 담당한다` 결정을 대체한다. Python은 CNN 원본을 종합점수와 정확히 7개 지표의 일관된 사실 구조로 정규화한다. AI는 이 7개를 모두 보고 주요 공포 요인 최대 2개, 완충 요인 최대 1개와 한 줄 해석을 직접 선정·설명한다. Python 검증기는 AI가 선택한 지표·점수·등급이 실제 입력에 존재하는지와 출력 형식·금지 범위만 확인한다.
- **이유**: 정규화는 복잡하고 변할 수 있는 CNN 응답을 안정된 입력으로 바꾸는 전처리이지 해석이 아니다. 주요 요인의 상대적 의미를 7개 전체 맥락에서 비교·설명하는 것이 AI를 사용하는 핵심 목적이다. 입력 근거 검증을 Python에 남기면 AI의 해석 역할을 유지하면서 없는 사실·뉴스·전망·매매 조언을 차단할 수 있다.
- **fallback**: 기존 점수 정렬 기반 Python 선정기와 정적 보고서는 AI 호출 실패, 비활성화 또는 출력 검증 실패 때만 사용한다. 정상 AI 요청에 이 선정 결과를 후보로 전달하지 않는다.
- **버린 대안**: Python이 공포 2개와 완충 1개를 먼저 확정하고 AI가 문장만 작성 — 7개 지표 전체를 비교해 요인을 판단하려는 기능 의도를 제한한다.

## 2026-07-20 — Gemini는 Interactions API와 이중 검증을 사용한다

- **결정**: Gemini는 REST Interactions API를 사용한다. API 키는 `x-goog-api-key` 헤더에만 넣고 모델 ID는 `LLM_MODEL` 환경변수에서 읽는다. `response_format` JSON schema로 문법 구조를 제한한 뒤, Python 검증기가 AI가 선택한 지표의 ID·명칭·점수·등급을 입력 7개와 다시 대조한다.
- **이유**: 공급자 구조화 출력은 JSON 형태를 안정시키지만 선택 근거가 실제 입력과 일치하는지까지 보장하지 않는다. 모델을 코드에 고정하지 않으면 배포 시점의 비용·지원 상태에 맞춰 교체할 수 있다.
- **복구 정책**: 형식 또는 근거 검증 실패는 입력 전체를 유지한 채 한 번만 재요청하고, 네트워크·HTTP 오류 또는 두 번째 검증 실패는 `TemplateProvider`로 전환한다.
- **버린 대안**: 구조화 출력만 신뢰하거나 API 키를 URL·본문에 포함 — 사실 불일치를 놓치거나 비밀값 노출 위험을 높인다.

## 2026-07-20 — DeepSeek는 공통 프롬프트·검증기를 재사용한다

- **결정**: DeepSeek는 별도 SDK 없이 공식 OpenAI 호환 REST `/chat/completions`를 호출한다. API 키는 `Authorization: Bearer` 헤더에만 넣고, `response_format={"type":"json_object"}`를 사용한다. Gemini와 동일한 7개 전체 입력 프롬프트와 `MarketComment` 파서·입력 근거 검증기·정적 fallback을 재사용한다.
- **이유**: 공급자별 차이는 인증·요청·응답 경계에만 가두고, AI의 역할과 안전 검증은 바뀌지 않게 해야 환경변수 교체가 실제 기능 차이를 만들지 않는다. HTTP 클라이언트를 이미 사용하므로 공급자 SDK 의존성도 불필요하다.
- **선택 방식**: `LLM_PROVIDER=gemini|deepseek|none`으로 고르고 모델은 공통 `LLM_MODEL`에서 읽는다. `none`은 외부 AI 호출 없이 `TemplateProvider`를 사용하며, 알 수 없는 값은 시작 시 설명 가능한 설정 오류로 거부한다.
- **버린 대안**: DeepSeek 전용 프롬프트·출력 모델·검증기를 복제하거나 모델 ID를 코드에 고정 — 공급자 간 동작이 어긋나고 교체 비용이 커진다.

## 2026-07-20 — 현재 운영은 Gemini만 사용하고 DeepSeek는 미연결로 둔다

- **결정**: 구현된 DeepSeek 공급자와 팩토리 테스트는 제거하지 않지만, 현재 배포·운영에서는 Gemini만 사용한다. DeepSeek API 키를 준비하거나 Secret Manager·실시간 검증에 연결하지 않으며 후속 단계에서 DeepSeek 전용 작업을 추가하지 않는다.
- **이유**: 현재 요구사항에는 실제 공급자 교체가 필요하지 않다. 이미 작성된 선택 경계는 추가 런타임 의존성이나 호출 비용이 없고 향후 확장에 쓸 수 있으므로 보존하되, Telegram·MCP·배포 완성에 우선순위를 둔다.
- **재검토 조건**: Gemini의 비용·장애·정책 변화로 실제 교체 필요가 생겼을 때만 DeepSeek 연결과 실시간 검증을 별도 승인 후 진행한다.

## 2026-07-20 — Telegram 미리보기는 순수 함수, 전송은 명시적 단일 호출로 분리한다

- **결정**: Telegram 문구는 MarkdownV2 escape와 길이 제한을 적용하는 순수 렌더러로 먼저 만든다. 렌더러와 Telegram 미리보기 계층은 Bot API를 호출하지 않고, 실제 `send`만 Bot API `sendMessage`를 호출한다. 상위 MCP 미리보기는 보고서 생성을 위해 CNN·시장시세를 조회할 수 있다. 실행 날짜 기반 idempotency key는 전송 계층까지 전달하지만 Telegram 요청 본문에는 넣지 않는다.
- **이유**: 사용자가 문구를 검수하기 전 오발송하는 일을 막고, Telegram 실패를 데이터 수집·AI 분석 성공과 분리하기 위해서다. Telegram은 애플리케이션 idempotency key를 지원하지 않으므로 향후 실행 ledger가 중복을 차단해야 한다.
- **재시도 정책**: timeout 뒤에는 Telegram 수신 여부가 불명확하므로 클라이언트가 자동 재시도하지 않는다. 11단계 중복 방지 저장소가 연결되기 전에는 상위 계층도 임의 재전송하지 않는다.
- **버린 대안**: 보고서 생성 함수 안에서 곧바로 전송하거나 timeout을 즉시 재시도 — 테스트 중 오발송과 중복 메시지 위험을 높인다.

## 2026-07-20 — MCP 전송은 검수한 미리보기와 해시로 결속한다

- **결정**: 공식 Python MCP SDK v1을 `mcp>=1.27,<2`로 고정해 FastMCP 도구 3개를 제공한다. 미리보기는 Telegram 원문과 SHA-256을 반환하고, 전송은 같은 원문·해시, `confirm_send=true`, 서버의 명시적 전송 활성화를 모두 요구한다.
- **이유**: preview와 send 사이에 CNN 또는 AI를 다시 호출하면 사용자가 보지 않은 새 문구가 전송될 수 있다. 원문과 해시를 함께 전달하면 검수한 문구를 그대로 보낸다는 계약을 테스트할 수 있다. MCP SDK v2는 아직 안정판 전이므로 의도치 않은 주 버전 업그레이드도 막는다.
- **공개 정책**: `MCP_ALLOW_TELEGRAM_SEND` 기본값은 false다. 원격 인증을 구현하기 전에는 전송 도구가 호출돼도 외부 메시지를 보낼 수 없다.
- **버린 대안**: send가 보고서를 다시 생성하거나 미리보기 없이 바로 전송 — AI 결과 변동과 익명 부수효과 위험이 있다.

## 2026-07-20 — 네이버 코스피와 Yahoo 주요 시세를 실패 격리 방식으로 결합한다

- **결정**: 코스피는 현재 응답이 검증된 네이버 모바일 기본 API를 우선 사용하고 실패하면 Yahoo `^KS11`로 대체한다. 달러·원, 비트코인, 나스닥·S&P 500 현물·선물은 Yahoo chart 응답을 각각 독립적으로 검증한다.
- **이유**: 기존 `main.py`의 시장시세 기능을 추가 런타임 의존성 없이 복원하고, 한 공급자나 한 심볼의 장애가 핵심 F&G 보고서와 정상 시세를 막지 않게 하기 위해서다.
- **버린 대안**: FinanceDataReader 같은 대형 수집 패키지를 즉시 필수 의존성으로 추가 — 외부 정책 변화와 패키지 장애가 아침 보고서 전체의 장애점이 될 수 있다.

## 2026-07-20 — 원본 기능은 정보성 아침 브리핑으로 합친다

- **결정**: MCP 기본 서비스의 `preview_telegram_report`와 HTTP `/tasks/daily-report`가 같은 통합 메시지를 만든다. 메시지에는 주요 시장시세, 현재·전일 F&G 변화, 기존 7개 지표 기반 설명을 포함한다. 원본 코드의 고정 매수·레버리지 추가 매수 문구는 포함하지 않는다.
- **이유**: 예약 작업과 수동 MCP 미리보기가 서로 다른 문구를 만들지 않게 하고, 사용자가 검수한 정확한 원문·해시를 그대로 Telegram에 보내는 기존 안전 계약을 유지하기 위해서다. 심리지표는 정보 제공 자료이지 투자 지시의 근거가 아니다.
- **운영 경계**: 코드는 통합 아침 보고서 생성까지 완료했다. 실제 정시 실행은 Cloud Run 배포 후 인증된 Cloud Scheduler와 영속 중복 방지 저장소를 연결한 다음 활성화한다.

## 2026-07-20 — 기존 소스 기반 Cloud Run 서비스는 단일 파일 배포본으로 갱신할 수 있다

- **결정**: 이미 운영 중인 Python 소스 기반 Cloud Run 서비스에는 `deploy/cloud-run-source/`의 `main.py`, `requirements.txt`, `Procfile`만 교체하는 간소화 경로를 제공한다. 서비스 이름·URL·기존 트리거는 유지하고 새 revision으로 배포한다.
- **구성**: `FinanceDataReader`와 `pytz`를 제거하고 Flask·requests·gunicorn만 사용한다. CNN 7개 지표, 네이버 코스피, Yahoo 주요 시세, Gemini REST 요약, Telegram 전송을 하나의 `main.py`에 둔다. Gemini 키가 없거나 호출·검증에 실패하면 정적 fallback을 사용한다.
- **안전 경계**: 첫 revision은 `ENABLE_TELEGRAM_SEND=false`로 배포하고 `/?send=false`에서 미리보기를 확인한 다음 전송을 켠다. Gemini·Telegram 비밀값은 코드가 아니라 환경변수 또는 Secret Manager 참조로 주입한다.
- **트레이드오프**: 단일 파일 경로는 기존 배포를 가장 적게 바꾸지만 `/mcp` 프로토콜과 영속 중복 방지 ledger를 제공하지 않는다. 원격 MCP와 인증된 Scheduler가 필요할 때는 별도 컨테이너 애플리케이션을 사용한다.

## 2026-07-20 — Google Cloud 경로를 폐기하고 로컬 MCP·Skill 프로젝트로 되돌린다

- **결정**: 앞선 Cloud Run·Cloud Scheduler·Secret Manager·Docker 운영 결정과 기존 소스 서비스 갱신 결정을 대체한다. 검증된 데이터·AI 요약·Telegram 안전 경계는 보존하되, `fng-chatbot` 코어와 로컬 stdio MCP, Codex·Claude Code용 Skill, `AGENTS.md`, `CLAUDE.md`를 최종 결과로 삼는다.
- **이유**: 원래 목적은 클라우드 서비스를 하나 더 운영하는 것이 아니라, 같은 로컬 도구를 MCP로 노출하고 Skill과 에이전트별 진입점으로 반복 활용하는 구조를 직접 만드는 것이었다. 현재 Cloud·HTTP 층은 이 목표보다 설치·인증·배포·운영 범위를 크게 만들고 실제 MCP·Skill 자동 연결은 비워 두었다.
- **제거 범위**: `deploy/`, Dockerfile, HTTP 앱과 테스트, Cloud 전용 의존성·환경변수·문서 지시를 제거한다. 과거 결정 기록은 삭제하지 않고 이 결정으로 대체되었음을 남긴다.
- **새 운영 경계**: MCP는 로컬 stdio만 지원한다. 실제 Telegram 전송은 기존 미리보기·해시·명시적 승인 경계를 유지한다. 자동 예약 실행과 실제 외부 전송은 별도 승인 전에는 범위 밖이다.
- **실행 결과**: 복구 기준점을 만든 뒤 Cloud·HTTP 파일을 제거하고 로컬 MCP·양쪽 Skill·에이전트 진입점을 연결했다. 일회성 재구성 계획서는 완료 후 제거했다.

## 2026-07-20 — 생성형 AI 공급자를 제거하고 규칙 기반 MCP·Skill로 완성한다

- **결정**: 앞선 Gemini·DeepSeek·LLM 해석 관련 결정을 대체한다. 보고서 요인은 정규화된 7개 지표를 Python 규칙으로 선택하고, 문장도 로컬 템플릿으로 만든다.
- **이유**: 로컬 MCP와 Skill을 빠르고 반복 가능하게 완성하는 것이 현재 목표이며, 짧은 정보성 해석에 별도 모델 API·키·비용·네트워크 의존이 필요하지 않다.
- **제거 범위**: `src/fng_chatbot/llm/`, 공급자 설정과 테스트, 모델 환경변수, 문서의 Gemini·DeepSeek 실행 지시를 제거한다.
- **보존 범위**: CNN·시장시세 수집, 규칙 기반 요인 선정, MCP 도구 3개, Telegram 미리보기·해시·명시적 승인 경계는 유지한다.
- **재검토 조건**: 향후 규칙 기반 해석으로 해결할 수 없는 구체적 요구가 생길 때만 별도 결정으로 생성형 AI 계층을 다시 검토한다.

## 2026-07-21 — Python이 요인을 정하고 DeepSeek는 Telegram 미리보기에서 의미만 설명한다

- **결정**: 이 결정은 `생성형 AI 공급자를 제거하고 규칙 기반 MCP·Skill로 완성한다`, `AI가 7개 지표 전체에서 요인을 직접 선정한다`, Gemini 기본·공통 공급자 선택 관련 결정을 대체한다. Python 규칙이 공포 요인 최대 2개와 완충 요인 최대 1개를 확정하고 규칙 요약을 만든다. `get_fear_greed_report`는 이 결정적 결과만 반환한다. `preview_telegram_report`에서만 `deepseek-v4-flash` 비사고 모드가 선택된 요인의 의미를 1~2줄·300자 이내로 설명한다.
- **검증 경계**: DeepSeek에는 선택된 요인의 역할·ID·명칭·점수·등급·근거만 전달한다. 응답은 JSON 객체의 `lines`와 `referenced_indicator_ids`만 허용하고, Python이 참조 ID 순서·길이·금지 표현을 검증한다. 뉴스·전망·매매 조언·URL은 금지한다.
- **fallback**: `DEEPSEEK_API_KEY` 누락, 통신·HTTP·응답·검증 오류와 예상 밖 공급자 오류는 Telegram 미리보기 전체를 실패시키지 않는다. 규칙 문장으로 복귀하고 비밀값 없는 사유 코드만 MCP 메타데이터에 기록한다. 모델명과 오류 원문은 Telegram 본문에 넣지 않는다.
- **비밀값**: DeepSeek 키와 Telegram 토큰·채팅 ID는 저장소 밖의 사용자 환경변수에 둔다. Codex·Claude MCP 설정에는 실제 값이 아닌 변수 이름 또는 `${VAR}` 참조만 둔다. `MCP_ALLOW_TELEGRAM_SEND=false`와 미리보기 승인·SHA-256 결속은 유지한다.
- **버린 대안**: Codex/Claude 서브에이전트가 해석문을 공개 MCP 인자로 주입하거나, DeepSeek가 7개 지표에서 요인을 다시 선택하거나, API 실패로 핵심 규칙 보고서까지 실패시키는 방식.

## 2026-07-21 — DeepSeek V4 Flash는 NVIDIA hosted NIM으로 호출한다

- **결정**: 이 결정은 앞선 DeepSeek 공식 API endpoint·`DEEPSEEK_API_KEY` 사용 부분을 대체한다. `preview_telegram_report`는 `NVIDIA_API_KEY`를 Bearer 인증에 사용해 `https://integrate.api.nvidia.com/v1/chat/completions`의 `deepseek-ai/deepseek-v4-flash`를 호출한다. 모델은 `reasoning_effort=none`, JSON object, 비스트리밍, 최대 256토큰으로 고정하고 공급자·모델 선택 계층은 추가하지 않는다.
- **이유**: 선택한 V4 Flash 모델은 NVIDIA API Catalog의 hosted endpoint와 모델 ID로 제공되며, NVIDIA 키를 DeepSeek 공식 endpoint에 보내면 공급자 불일치로 인증에 실패한다. 환경변수와 endpoint를 같은 공급자 경계로 결속해 재발을 막는다.
- **호환·fallback**: 공개 MCP 도구와 Python 요인 선정·출력 검증·Telegram 미리보기 해시·승인 전송 계약은 바꾸지 않는다. `NVIDIA_API_KEY` 누락, 통신·HTTP·응답·검증 실패는 기존처럼 규칙 문장과 비밀값 없는 사유 코드로 격리한다. 기존 `DEEPSEEK_API_KEY`는 읽지 않는다.
- **비밀값**: 실제 NVIDIA 키는 저장소 밖의 Windows 사용자 환경변수에만 두고 Codex는 변수 이름, Claude는 `${NVIDIA_API_KEY:-}` 참조만 저장한다. 키 원문·인증 헤더·공급자 오류 원문을 코드·문서·fixture·로그·Telegram 본문에 남기지 않는다.

## 2026-07-21 — DeepSeek 실패는 세부 코드로 관측하고 일시 장애만 한 번 재시도한다

- **결정**: 해석 검증 오류는 JSON·키·타입·줄 수·길이·참조 ID·금지 표현별 고정 코드로, 전송 오류는 connect/read/write/pool timeout과 connect/read/write/close/proxy/network 오류별 고정 코드로 구분해 MCP `interpretation.reason`에 기록한다. 알 수 없는 코드는 각각 `validation_error`, `transport_error`로 축소한다.
- **재시도 경계**: NVIDIA 요청 timeout은 10초에서 20초로 늘린다. read timeout과 HTTP 429·5xx만 총 2회까지 시도하며 연결·쓰기 오류, 401·403, 응답 envelope 오류, 해석 검증 오류는 재시도하지 않는다. 이 재시도는 Telegram을 보내지 않는 읽기 전용 미리보기의 모델 설명 생성에만 적용된다.
- **이유**: 실제 실행에서 HTTP 200 뒤 검증 실패, read timeout, HTTP 503이 같은 일반 fallback 코드로 뭉쳐 원인을 구분할 수 없었다. 안전한 단계 코드로 진단 가능성을 높이고 일시적인 NVIDIA 장애만 제한적으로 흡수하면 검증 계약과 비밀값 경계를 느슨하게 하지 않고 설명 적용 성공률을 높일 수 있다.
- **버린 대안**: 원시 모델 응답·예외 원문을 로그에 남기거나 모든 오류를 무조건 재시도하거나 검증 조건을 완화하는 방식. 이는 비밀값·공급자 세부 노출, 중복 비용, 근거 없는 설명 수용 위험을 높인다.

## 2026-07-21 — 공용 추론 풀 포화는 backoff로 흡수하고 거절 사유는 고정 어휘로 남긴다

- **배경**: 반복된 `http_503`을 실측 진단한 결과 키·모델·payload는 모두 정상이었다. 앱과 동일한 요청이 HTTP 200을 반환했고, 503 본문은 `ResourceExhausted: Worker local total request limit reached (48/48)`로 NVIDIA 공용 워커 풀의 동시 요청 포화였다. 실패의 직접 원인은 공급자가 아니라 대기 없이 즉시 재시도하던 클라이언트 구조였다. 실측 성공률은 무대기 2회에서 5/6, 지수 backoff 4회에서 6/6이었다.
- **결정**: 재시도 사이에 1·2·4초 지수 backoff와 25% jitter를 두고 최대 시도를 2회에서 4회로 올린다. jitter는 동시에 실행된 미리보기가 같은 포화 워커로 다시 몰리는 것을 막는다. 재시도 판단 대상(read timeout, 429, 5xx)과 비재시도 계약(연결·쓰기 오류, 401·403, 응답 envelope, 해석 검증)은 그대로 둔다.
- **시간 예산**: 시도 4회가 각각 20초 timeout에 걸리면 backoff까지 87초로 MCP 도구 90초 예산을 넘길 수 있으므로, 클라이언트가 누적 45초를 넘길 재시도를 스스로 포기한다. 이렇게 해야 우아한 규칙 fallback이 MCP 도구 실패로 악화되지 않는다.
- **거절 사유**: HTTP 거절 본문을 `resource_exhausted`·`rate_limited`·`model_unavailable`·`unauthorized`·`invalid_request`·`unspecified` 여섯 개 고정 어휘로만 요약해 `http_503_resource_exhausted` 형태로 기록한다. 본문 원문은 보존하지 않으므로 키·토큰·요청 식별자가 메타데이터로 새어나갈 수 없고, 어휘 밖 값은 `unspecified`로 축소한다.
- **이유**: 기존 `http_503`만으로는 용량 포화와 설정 오류를 구분할 수 없어 같은 진단을 여러 세션에서 반복했다. 상태 코드에 고정 어휘 한 단어를 붙이면 다음 실패에서 즉시 구분된다.
- **버린 대안**: 공급자 오류 본문을 그대로 사유에 담기 — 비밀값·요청 식별자 노출 위험이 있다. 무제한 재시도나 시간 예산 없는 상향 — 비용과 MCP 도구 timeout 위험을 키운다. `Retry-After` 헤더 우선 처리 — HTTP 응답 계약에 헤더 접근을 추가해야 하므로 이번 범위에서 제외했다.

## 2026-07-21 — 금지 표현 검증을 제거하고 매매 권유 배제는 프롬프트에만 맡긴다

- **대체 관계**: 2026-07-21 「DeepSeek 실패는 세부 코드로 관측하고…」의 금지 표현 사유 코드와 2026-07-21 「Python이 요인을 정하고…」의 금지 표현 검증 항목을 이 결정이 대체한다. 앞선 기록은 당시 사실이므로 수정하지 않는다.
- **배경**: 실측에서 5회 중 2회가 `validation_forbidden_language`로 폐기됐고 원인은 `매도` 한 단어였다. 걸린 문장은 "하락 종목 거래량이 상승 종목을 크게 압도해 매도 심리가 극도로 확산되었습니다"로, 매도를 권유하는 문장이 아니라 시장 상황을 서술하는 정상 출력이었다. 부분 문자열 매칭이라 서술형 표현과 권유형 표현을 구분하지 못했다.
- **결정**: `_FORBIDDEN_PHRASES` 상수, `validate_interpretation`의 금지 표현 검사, `validation_forbidden_language` 사유 코드와 해당 테스트를 제거한다. 남는 검증은 JSON 구조, 허용 키, 배열 타입, 1~2줄, 줄바꿈 포함 300자, 참조 ID 순서 일치, 모델명 존재다.
- **남기는 장치**: 시스템 프롬프트의 "뉴스·기사·외부 사실·가격 전망·수익률 예측·매수·매도·종목 추천·URL은 사용하지 마세요" 지시는 유지한다. Python이 요인을 결정하고 DeepSeek는 그 의미만 설명하는 구조, 결정적 규칙 요약에 대한 `test_report.py`의 매매 표현 금지 검사도 그대로 둔다.
- **받아들인 위험**: 이제 매수·매도 권유를 막는 강제 장치가 없고 프롬프트 지시만 남는다. DeepSeek가 권유형 문장을 반환하면 Telegram 미리보기에 그대로 들어갈 수 있다. 전송 전 사용자 승인과 SHA-256 결속이 마지막 검수 지점이므로 미리보기 문구를 육안으로 확인해야 한다.
- **버린 대안**: 서술형을 허용하고 권유형 어미·조합만 정규식으로 차단하기 — 강제 장치를 유지하면서 오탐을 없앨 수 있었으나 사용자가 전체 삭제를 선택했다. `매도`·`매수`만 목록에서 빼기 — `매수 추천`이 `추천`에만 걸리는 등 경계가 어중간해진다.
